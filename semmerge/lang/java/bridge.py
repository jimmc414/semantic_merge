"""Java backend placeholder."""


class JavaWorker:
    def build_and_diff(self, base_tree, left_tree, right_tree):
        raise NotImplementedError(
            "Enable the Java backend by integrating a compiler frontend and exposing the protocol over stdio"
        )
